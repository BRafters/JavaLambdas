package com.company.abstractFunctions;

public interface Adder {
    // You can have additional methods in this interface
    // As long as you have a single abstract method, we refer to an interface
    // as a functional interface
    int add(int x, int y);
}
